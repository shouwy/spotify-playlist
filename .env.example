# Exemple de fichier .env — remplis avec tes valeurs localement (NE PAS committer de secrets)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# URL utilisée pour les callbacks OAuth en local (Netlify Dev écoute par défaut sur 8888)
SITE_URL=http://127.0.0.1:8888

# Upstash (si utilisé pour stocker le refresh token)
UPSTASH_REDIS_REST_URL=https://your-upstash-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Optionnel : forcer l'installation des devDependencies en CI si besoin
# NPM_CONFIG_PRODUCTION=false